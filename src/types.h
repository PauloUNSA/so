#ifndef __TYPES_H
#define __TYPES_H

typedef char int8_t;                     // Entero de 8 bits
typedef short int16_t;                   // Entero de 16 bits
typedef int int32_t;                     // Entero de 32 bits
typedef long long int int64_t;           // Entero de 64 bits
typedef unsigned char uint8_t;           // Entero de 8 bits sin signo
typedef unsigned short uint16_t;         // Entero de 16 bits sin signo
typedef unsigned int uint32_t;           // Entero de 32 bits sin signo
typedef unsigned long long int uint64_t; // Entero de 64 bits sin signo

#endif